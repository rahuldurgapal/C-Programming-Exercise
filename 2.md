Write a program that:

Takes input for multiple students’ marks (use loop).

Use if-else and logical operators to check if marks are valid (0-100).

Calculate average using arithmetic operator.

Print Pass/Fail using conditional operator.

Use switch to assign grades.

Stop entering students if user enters -1 (use break).

Skip student entry if marks < 0 or > 100 (use continue).





